# health-risk-api
Health Risk API A simple Flask-based REST API that estimates health risk using basic blood test values (glucose, haemoglobin, cholesterol) and returns a risk level (Low/Moderate/High) plus a human-readable remark, for use by a health prediction application.
